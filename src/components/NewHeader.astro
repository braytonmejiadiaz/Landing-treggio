<header>
  <nav class="fixed w-full z-20 top-0 start-0 bg-white bg-opacity-50 animate-nav-shadow">
    <div class="w-full max-w-[1240px] flex justify-between items-center mx-auto py-4">
      <!-- Logo -->
      <a href="/#inicio" class="w-1/4" id="logo-container">
        <img src="/TREGGIO-1.svg" alt="treggio" class="w-2/3" id="logo">
      </a>

      <!-- Menú y botones -->
      <div class="flex items-center gap-4 flex-grow justify-end">
        <!-- Menú principal -->
        <div
          id="navbar-sticky"
          class="md:flex items-center justify-end w-full md:w-auto hidden md:block"
        >
          <ul
            class="flex flex-col p-4 md:p-0 mt-4 border border-gray-100 rounded-lg md:space-x-8
           rtl:space-x-reverse md:flex-row md:mt-0 md:border-0 
            dark:border-gray-700 text-lg font-semibold items-center"
          >
            <li>
              <a
                href="/#inicio"
                class="btn block py-2 px-3 text-black/80 rounded md:bg-transparent md:p-0 hover:text-[#85A0E7]"
                aria-current="page">Inicio</a
              >
            </li>
            <li>
              <a
                href="/#sobre-nosotros"
                class="btn block py-2 px-3 text-black/80 rounded md:bg-transparent md:p-0 hover:text-[#85A0E7]">Sobre
                nosotros</a
              >
            </li>
            <li>
              <a
                href="/#nuestros-planes"
                class="btn block py-2 px-3 text-black/80 rounded md:bg-transparent md:p-0 hover:text-[#85A0E7]">Nuestros
                planes</a
              >
            </li>
            <li>
              <a
                href="https://treggio.co/blog"
                class="btn block py-2 px-3 text-black/80 rounded md:bg-transparent md:p-0 hover:text-[#85A0E7]">Blog</a
              >
            </li>
            <li>
              <a
                href="mailto:infotreggio@gmail.com"
                class="btn block py-2 px-3 text-black/80 rounded md:bg-transparent md:p-0 hover:text-[#85A0E7]">Contacto</a
              >
            </li>
            <!-- Botones "Ingresar" y "Crear cuenta" dentro del menú -->
            <li class="md:hidden">
              <a
                href="https://app.foodlogic.com.co/login"
                class="btn block py-2 px-3 text-white rounded font-bold transition-all duration-300">
                <span
                  class="bg-[#009974] p-2 rounded-full px-5 hover:bg-[#85A0E7] hover:scale-105 transition-all duration-300"
                  >Ingresar</span
                >
              </a>
            </li>
            <li class="md:hidden">
              <a
                href="https://app.foodlogic.com.co/registro"
                class="btn block py-2 px-3 text-white rounded font-bold transition-all duration-300">
                <span
                  class="bg-[#009974] p-2 rounded-full px-5 hover:bg-[#85A0E7] hover:scale-105 transition-all duration-300"
                  >Crear cuenta</span
                >
              </a>
            </li>
          </ul>
        </div>

        <!-- Botones "Ingresar" y "Crear cuenta" fuera del menú (para desktop) -->
        <div class="flex items-center gap-2 md:flex" id="desktop-buttons">
          <a
            href="https://app.treggio.co/ingresar"
            class="btn block py-2 px-3 text-white rounded font-bold transition-all duration-300 hidden md:block">
            <span
              class="bg-[#009974] p-2 rounded-full px-5 hover:bg-[#85A0E7] hover:scale-105 transition-all duration-300"
              >Ingresar</span
            >
          </a>
          <a
            href="https://app.treggio.co/registro"
            class="btn block py-2 px-3 text-white rounded font-bold transition-all duration-300 hidden md:block">
            <span
              class="bg-[#009974] p-2 rounded-full px-5 hover:bg-[#85A0E7] hover:scale-105 transition-all duration-300"
              >Crear cuenta</span
            >
          </a>
        </div>
      </div>

      <!-- Botón de menú móvil -->
      <div class="flex md:order-2 space-x-3 md:space-x-0 rtl:space-x-reverse">
        <a
          id="menu-button"
          data-collapse-toggle="navbar-sticky"
          data-nav-toggle="#navbar-sticky"
          href="#"
          class="inline-flex items-center p-2 w-10 h-10 justify-center text-sm text-gray-500 rounded-lg md:hidden
            hover:bg-gray-100 focus:outline-none focus:ring-2 focus:ring-gray-200 dark:text-gray-400 dark:hover:bg-gray-700 dark:focus:ring-gray-600"
          aria-controls="navbar-sticky"
          aria-expanded="false"
        >
          <svg
            class="w-5 h-5"
            aria-hidden="true"
            xmlns="http://www.w3.org/2000/svg"
            fill="none"
            viewBox="0 0 17 14"
          >
            <path
              stroke="currentColor"
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M1 1h15M1 7h15M1 13h15"
            ></path>
          </svg>
        </a>
      </div>
    </div>
  </nav>
</header>

<style>
  @keyframes nav-shadow {
    0% {
      background-color: rgba(255, 255, 255, 0);
    }
    to {
      box-shadow: 0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1);
      backdrop-filter: blur(10px);
      border: 1px solid rgba(255, 255, 255, 0.1);
    }
  }

  .animate-nav-shadow {
    animation: nav-shadow 1s linear both;
    animation-timeline: scroll();
    animation-range: 0 100px;
  }

  /* Estilos iniciales para el menú móvil */
  #navbar-sticky {
    display: none; /* Ocultar por defecto */
  }

  /* Estilos para el menú móvil cuando está activo */
  #navbar-sticky.active {
    display: block; /* Mostrar cuando la clase 'active' está presente */
    position: absolute;
    top: 100%;
    left: 0;
    right: 0;
    background-color: rgba(255, 255, 255, 0.9);
    backdrop-filter: blur(10px);
    z-index: 10;
  }

  /* Mostrar menú en pantallas más grandes (md breakpoint) */
  @media (min-width: 768px) {
    #navbar-sticky {
      display: flex; /* Mostrar el menú en pantallas medianas y grandes */
      position: static; /* Revertir a la posición estática */
      background-color: transparent; /* Eliminar el fondo */
      backdrop-filter: none; /* Eliminar el desenfoque */
      border: none; /* Eliminar el borde */
    }
  }

  /* Estilos para el logo en pantallas pequeñas */
  @media (max-width: 767px) {
    .w-full {
      display: flex;
      justify-content: center; /* Centrar horizontalmente */
    }

    #logo-container {
      width: auto; /* Ancho automático para el contenedor del logo */
      margin: 0 auto; /* Centrar horizontalmente */
    }

    #logo {
      width: 75%; /* Hacer el logo más grande */
    }
  }
</style>

<script>
  // Script para mostrar/ocultar el menú en móviles
  document.addEventListener('DOMContentLoaded', function () {
    const menuButton = document.getElementById('menu-button');
    const navbar = document.getElementById('navbar-sticky');

    if (menuButton && navbar) {
      menuButton.addEventListener('click', function () {
        navbar.classList.toggle('active');
      });
    } else {
      console.error('No se encontró el botón de menú o el menú.');
    }
  });
</script>
