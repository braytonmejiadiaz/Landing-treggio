<section class="w-full relative flex justify-center items-center mt-15 px-4 md:px-0">
    <div class="bg-gradient-to-r from-[#F1C671] via-white to-[#829DE7] w-full max-w-[1240px] mx-auto rounded-3xl h-auto md:h-90 relative overflow-visible flex flex-col md:flex-row items-center shadow-2xl py-8 md:py-0">
      <!-- Contenedor de botones -->
      <div class="flex flex-col md:flex-row gap-8 md:gap-14 items-center w-full md:w-auto md:ml-20"> <!-- Agregado de md:ml-20 -->
        <!-- Columna izquierda con los botones flotantes -->
        <div class="flex flex-col gap-6 md:gap-14 w-full md:w-auto">
          <!-- Botón de Pago Confirmado (flotante) -->
          <div class="flex items-center gap-3 bg-white text-black px-4 py-2 md:px-6 md:py-3 rounded-full shadow-lg text-base md:text-lg font-semibold rotate-[-5deg] animate-floating w-full md:w-auto">
            <div class="bg-green-500 p-2 md:p-3 rounded-full text-white flex items-center justify-center">
              <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5 md:w-6 md:h-6" viewBox="0 0 24 24" fill="none"
               stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M20 6 9 17l-5-5"/>
              </svg>
            </div>
            Pago confirmado
          </div>
  
          <!-- Botón +300 Usuarios (flotante) -->
          <div class="flex items-center gap-3 bg-white text-black px-4 py-2 md:px-6 md:py-3 rounded-full shadow-lg text-base md:text-lg font-semibold rotate-[-3deg] animate-floating delay-1000 w-full md:w-auto">
            <div class="bg-green-500 p-2 md:p-3 rounded-full text-white flex items-center justify-center">
              <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5 md:w-6 md:h-6" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M5 20a2 2 0 0 1-2-2v-2a4 4 0 0 1 4-4M19 20a2 2 0 0 0 2-2v-2a4 4 0 0 0-4-4M16 3.13a4 4 0 0 1 0 7.75M8 3.13a4 4 0 0 0 0 7.75M12 12a4 4 0 1 1 0-8 4 4 0 0 1 0 8Z"/>
              </svg>
            </div>
            +860 usuarios
          </div>
        </div>
  
        <!-- Columna derecha con el botón fijo -->
        <div class="flex justify-center w-full md:w-auto">
          <!-- Botón de Descarga App (fijo en el centro vertical) -->
          <div class="flex items-center gap-3 bg-white text-black px-4 py-2 md:px-6 md:py-3 rounded-full shadow-lg text-base md:text-lg font-semibold rotate-[5deg] w-full md:w-auto">
            <div class="bg-[#FC9A09] p-2 md:p-3 rounded-full text-white flex items-center justify-center">
              <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5 md:w-6 md:h-6" viewBox="0 0 24 24" fill="none" stroke="currentColor"
               stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M12 5v14M19 12l-7 7-7-7"/>
              </svg>
            </div>
            Descarga App
          </div>
        </div>
      </div>
  
      <!-- Imagen alineada abajo a la derecha -->
      <img src="/mano.png" alt="" class="w-full md:w-3/5 z-10 mt-8 md:mt-0 md:absolute md:bottom-0 md:right-0">
    </div>
  </section>
  
  <!-- Animación de flotación -->
  <style>
    @keyframes floating {
      0% { transform: translateY(0); }
      50% { transform: translateY(-10px); }
      100% { transform: translateY(0); }
    }
    .animate-floating {
      animation: floating 3s ease-in-out infinite;
    }
  
    /* Ajustes para móviles */
    @media (max-width: 768px) {
      .bg-gradient-to-r {
        padding: 4rem 2rem;
      }
      .flex-col > div {
        margin-bottom: 1rem;
      }
      .bg-white {
        padding: 0.5rem 1rem;
      }
      .bg-green-500, .bg-[#FC9A09] {
        padding: 0.5rem;
      }
      .w-5, .w-6 {
        width: 3rem;
      }
      .h-5, .h-6 {
        height: 3rem;
      }
    }
  </style>
  